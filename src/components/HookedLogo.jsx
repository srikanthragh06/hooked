const HookedLogo = ({ className }) => {
    return (
        <div
            className={`text-5xl text-yellow-600 relative 
        bungee-shade-regular font-bold
        px-2 py-1 ${className}`}
        >
            Hooked
        </div>
    );
};

export default HookedLogo;
